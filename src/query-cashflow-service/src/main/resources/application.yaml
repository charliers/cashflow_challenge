spring:
  application:
    name: query-cashflow-service

  threads:
    virtual:
      enabled: true

server:
  port: 8080
  servlet:
    context-path: /cashflow

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics